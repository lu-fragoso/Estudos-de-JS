let milAoContrario = '1'

let dezMil = "1"

console.log(milAoContrario.padStart(4,"0"))

console.log(dezMil.padEnd(5, "0"))


//adiciona caracteres no inicio da string 
// pode ser utilizado para corrigir algum dado que não foi colocado corretamente 

// metodo padEnd faz o mesmo só que no final

// padStart( "quantidadede de caracteres na string final", " elemenstos que serão adicionados")